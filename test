#!/bin/bash

# CHANGE THIS to where the Gitolite lib dir is:
GL_LIBDIR=/some/where/gitolite/src/lib
# Test repetition.
INTERVAL=1
export GL_LIBDIR

while [ : ]
do
    clear
    prove --color -v
    sleep ${INTERVAL}
done
